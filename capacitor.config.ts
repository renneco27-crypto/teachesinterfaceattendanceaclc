import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aclc.attendance',
  appName: 'ACLC Attendance',
  webDir: 'web-app/dist'
};

export default config;
